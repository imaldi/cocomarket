{
  "name": "cocomarket_frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "build:android": "vue-tsc && vite build && cordova build android --prod --release --buildConfig=build.json"
  },
  "dependencies": {
    "vue": "^3.4.19",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "cordova-android": "^12.0.1",
    "sass": "^1.71.1",
    "typescript": "^5.2.2",
    "vite": "^5.1.4",
    "vue-tsc": "^1.8.27"
  },
  "cordova": {
    "platforms": [
      "android"
    ]
  }
}